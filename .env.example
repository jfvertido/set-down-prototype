# Used by api/park.ts in phase 3. Set it in Vercel project settings, not in the client.
ANTHROPIC_API_KEY=
# Only needed if your API key isn't scoped to a workspace (the API will say so).
# Find the ID in the Claude Console under Settings > Workspaces.
ANTHROPIC_WORKSPACE_ID=
